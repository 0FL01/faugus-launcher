# Faugus Launcher - Структура Репозитория

## Описание Проекта

**Faugus Launcher** — это легковесный лаунчер для запуска Windows-игр на Linux с использованием UMU-Launcher.

⚠️ **Текущее состояние:** Проект находится в стадии активной миграции с **Python (GTK3)** на **Rust (Iced)**.
Степень готовности Rust-версии: ~75-80% (на январь 2026).

- **Лицензия**: MIT License
- **Язык**: Rust (активная разработка), Python 3 (legacy)

## Структура Директорий

### 🦀 Rust (Новая версия)
```
faugus-launcher/
├── Cargo.toml                   # Зависимости Rust проекта
├── src/
│   ├── main.rs                  # Точка входа приложения
│   ├── config/                  # Настройки (приложение, игры, пути)
│   │   ├── app_config.rs        # Конфигурация приложения (тема, язык)
│   │   ├── game_config.rs       # Структуры игр + совместимость с legacy
│   │   └── paths.rs             # XDG пути (config, data, cache)
│   ├── gui/                     # Интерфейс на Iced (окна, диалоги)
│   │   ├── add_game_dialog.rs   # Диалог добавления/редактирования игр
│   │   ├── confirmation_dialog.rs # Диалог подтверждения (удаление и др.)
│   │   ├── context_menu.rs      # Контекстное меню игр (Play, Edit, Logs...)
│   │   ├── file_picker.rs       # Обертка над rfd для выбора файлов/папок
│   │   ├── log_viewer_dialog.rs # Просмотр логов запуска
│   │   ├── main_window.rs       # Главное окно (список/сетка/баннеры)
│   │   ├── proton_manager_dialog.rs # Менеджер версий Proton
│   │   ├── settings_dialog.rs   # Настройки приложения
│   │   └── styles.rs            # Стилизация (Deep Space тема)
│   ├── launcher/                # Логика запуска (UMU, MangoHud, GameMode)
│   │   ├── game_launcher.rs     # Низкоуровневый запуск процессов (umu-run)
│   │   ├── launch_controller.rs # Высокоуровневый контроллер состояния (UI интеграция)
│   │   └── wine_tools.rs        # Инструменты для Wine/Proton
│   ├── locale/                  # Система локализации (i18n)
│   │   └── i18n.rs              # Мультиязычность (fluent-based)
│   ├── proton/                  # Менеджер версий Proton
│   │   ├── proton_manager.rs    # Скачивание/установка Proton (GE, EM)
│   │   └── runner_resolver.rs   # Резолвер Proton runner-ов
│   ├── shortcuts/               # Создание .desktop файлов
│   │   └── desktop_entry.rs     # Генерация .desktop entry файлов
│   ├── steam/                   # Интеграция со Steam (VDF)
│   │   └── shortcuts.rs         # Чтение/запись shortcuts.vdf
│   ├── tray/                    # Системный трей (tray-icon)
│   │   ├── icon.rs              # Управление иконкой трея
│   │   ├── menu.rs              # Меню трея
│   │   └── tray.rs              # Основная логика трея
│   ├── icons/                   # Менеджер иконок
│   │   └── icon_manager.rs      # Извлечение/кэширование иконок игр
│   └── utils/                   # Компоненты (EAC/BattlEye)
│       └── components.rs        # Утилиты античитов
```

### 🐍 Python (Legacy версия 1.13.5)
```
faugus-launcher/
├── faugus_launcher.py           # Главное приложение GTK3
├── faugus_run.py                # Скрипт запуска игр
├── faugus_proton_manager.py     # Менеджер Proton
├── faugus/                      # Модули (config, shortcuts, components)
└── languages/                   # Файлы переводов (gettext)
```

## Основные Компоненты (Rust)

**1. Графический интерфейс (`src/gui/`)**
- Реализован на **Iced**.
- Включает главное окно (список/сетка/баннеры), настройки, диалоги добавления игр.
- Полная асинхронность UI.
- **Модули**:
  - `add_game_dialog.rs` — форма добавления/редактирования игр (EXE, WinePREFIX, аргументы)
  - `confirmation_dialog.rs` — универсальный диалог подтверждения действий
  - `context_menu.rs` — контекстное меню для игр (Play, Edit, Logs, Hide)
  - `file_picker.rs` — обертка над `rfd` для нативного выбора файлов/папок
  - `proton_manager_dialog.rs` — менеджер версий Proton (скачивание GE/EM)
  - `styles.rs` — Deep Space тема (цвета, стили кнопок/полей)

**2. Запуск игр (`src/launcher/`)**
- Интеграция с **UMU-Launcher**.
- Настройка `WINEPREFIX`, `PROTONPATH`.
- Поддержка опций: MangoHud, GameMode, Gamescope.
- Управление процессами (мониторинг PID, kill).
- **Модули**:
  - `game_launcher.rs` — низкоуровневый запуск процессов (umu-run, env setup)
  - `launch_controller.rs` — высокоуровневый контроллер состояния (UI интеграция, async Task)
  - `wine_tools.rs` — утилиты для Wine/Proton

**3. Интеграция (`src/steam/`, `src/tray/`, `src/shortcuts/`)**
- Чтение/запись `shortcuts.vdf` Steam.
- Полнофункциональный системный трей (сворачивание, уведомления).
- Генерация .desktop файлов.
- **Модули**:
  - `steam/shortcuts.rs` — VDF парсер для Steam shortcuts
  - `tray/icon.rs` — управление иконкой трея
  - `tray/menu.rs` — меню трея
  - `shortcuts/desktop_entry.rs` — генерация .desktop entry

**4. Утилиты (`src/utils/`, `src/proton/`, `src/icons/`)**
- Загрузка UMU-Launcher.
- Заготовки для античитов (EAC/BattlEye).
- Скачивание/установка Proton (GE, EM).
- Извлечение и кэширование иконок игр.
- **Модули**:
  - `proton/proton_manager.rs` — загрузка/установка Proton версий
  - `proton/runner_resolver.rs` — резолвер Proton runner-ов
  - `icons/icon_manager.rs` — извлечение иконок из EXE/ICO файлов
  - `utils/components.rs` — утилиты античитов

## Технологии и Зависимости

### Rust Stack
- **GUI**: `iced` (v0.13) — кроссплатформенный GUI.
- **Async**: `tokio`, `futures`.
- **Сериализация**: `serde`, `serde_json`, `keyvalues-serde`, `new-vdf-parser` (Valve Data Format).
- **Система**: `sysinfo` (процессы), `directories` (XDG пути), `tray-icon`, `nix` (Linux syscalls).
- **Файловая система**: `fs2` (блокировка), `walkdir` (обход директорий).
- **Архивы**: `flate2`, `tar` (распаковка Proton).
- **Сеть**: `reqwest`.
- **Трей/Уведомления**: `tray-icon`, `muda` (меню), `notify-rust` (уведомления), `gtk` (интеграция).
- **Графика**: `image` (обработка иконок).
- **Файловые диалоги**: `rfd` (нативный выбор файлов/папок).
- **Логирование**: `tracing`, `tracing-subscriber`.
- **Ошибки**: `anyhow`, `thiserror`.
- **Утилиты**: `uuid` (генерация идентификаторов), `open` (открытие файлов/папок).

### Python Stack (Legacy)
- **GUI**: GTK3 (PyGObject).
- **Сборка**: Meson + Ninja.
- **Зависимости**: `psutil`, `requests`, `vdf`, `AyatanaAppIndicator3`.

### Rust
```bash
cargo build --release
```

### Python (Legacy)
```bash
meson setup build
ninja -C build
ninja -C build install
```

## Пути (XDG)

Приложение использует стандартные XDG пути, совместимые с обеими версиями (Rust/Python):

**Конфигурация** (`~/.config/faugus-launcher/`):
- `config.ini` — настройки приложения (тема, язык)
- `games.json` — список игр
- `latest-games.txt` — последние запущенные игры
- `icons/` — кэш иконок игр
- `banners/` — баннеры для сетки
- `logs/` — логи запуска игр
- `envar.txt` — переменные окружения (legacy compat)

**Данные** (`~/.local/share/faugus-launcher/`):
- `faugus-launcher.lock` — блокировка запуска (fs2)
- `running_games.json` — состояние активных процессов

**Steam интеграция**:
- `~/.local/share/Steam/userdata/<id>/config/shortcuts.vdf`
- Steam Library: `~/.local/share/Steam/steamapps/common/`
